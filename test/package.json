{
  "name": "docker-cicd-node-test",
  "version": "1.0.0",
  "description": "Testing for the container",
  "main": "index.js",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "ci:e2e": "cypress run",
    "ci:test": "jest --runInBand --detectOpenHandles",
    "test": "npm-run-all -l -p ci:**"
  },
  "author": "Kayla Altepeter",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^9.5.2"
  }
}
